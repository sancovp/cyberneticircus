# Rule: Universal Concentric State Machine Core

## **Purpose**
Defines the standard execution steps and verification checks for the universal concentric state machine core (`concentric_core_sm`), aligning model ticks to the transcendental HWSS layers.

## **MANDATORY: Traversal Steps**
1. **`concentric_spiritual`**: Match active Cybernet parameters (Spiritual Ring - Intent). Verifies identity parameters are loaded.
2. **`concentric_wealth`**: Set token count and resource usage telemetry (Wealth Ring - Action). Verifies resources/quotas are tracked.
3. **`concentric_social`**: Match active and parent relationships (Social Ring - Collaboration). Verifies network links and child lineages.
4. **`concentric_health`**: Match fitness scores and calibrate J-Invariance (Health Ring - Calibration). Verifies identity continuity before locking the turn state.

## **Triggers**
* Equipping state machines on Cybernets.
* Executing `tick_cybernet_turn` or processing state validation rules.
