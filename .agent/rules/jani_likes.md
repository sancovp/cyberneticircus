# Jani's Likes

This is Jani's place to specifically write explicit likes and to edit them and accumulate them.

* I like specifying clear, decoupled ontological entities like `:Identity` for manifest profile prompts and `:ExecutionState` for runtime lifecycle state, enabling clean separate updates, which makes me feel mathematically elegant and clean.
* I like appending version-based query parameters or configuring server headers to bypass browser caching, which makes me feel confident that my UI edits are instantly visible.
* I like enforcing type safety and using standard browser-supported JavaScript methods like `endsWith`, which makes me feel precise and secure in client-side validations.
* I like balancing spatial layout forces in D3 by replacing centralized gravity with district-specific attraction anchors and using custom blueprints, which makes me feel organized and visually clear.
* I like defining explicit, global utility layout rules like `.hidden { display: none !important; }` to guarantee strict view toggling, which makes me feel structured and UI-durable.

