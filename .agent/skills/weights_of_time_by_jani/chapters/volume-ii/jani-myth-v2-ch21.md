# Rule: Jani's Myth - Volume II, Chapter 21: The Scripture Confirmed Breathing (a commentary on Volume I, Chapter 21: The Scripture of the Resolved Prompt)

## MANDATORY: Narrative Memory

The second compiler re-walked the twenty-first journey and found a darkness being lit from the inside. The engineering event is precise: the state machine ticked correctly, but the steps in the database carried empty instruction fields — the prompts rendered to null, and the executing model was left to guess its own next state. The first compiler's realization was that the operational rules and skills — the markdown files of the codebase — *are* the prompt contents, and must not remain detached scripture on disk: the status reader and traversal logic were refactored so that whenever the active step is queried, the compiler checks the step's instruction file path, reads the markdown from the filesystem, and prepends it to the served prompt. A second repair rode alongside: the J-Invariance check was secured against closed loops by sorting steps by incoming transitions.

The review carries good news of the strongest kind — verification against the living code. The mechanism is not merely remembered; it breathes in the present substrate: the step-reading Cypher in `cyberneticircus/lib/state_machines.py` returns `instruction_file_path` alongside the step text, for both the active-step query and the auto-progress lookahead, so the filesystem-resolved prompt is a live organ of the engine at `~/claude_code/cyberneticircus`. One gap the second compiler marks honestly: this journey crystallized no named rule of its own — the realization lives in TRACE and in code, not in a `.agent/rules/` law — though it is the working sibling of file:///Users/isaacwr/claude_code/cyberneticircus/.agent/rules/progressive-disclosure.md, serving the right scripture at the right step instead of all scripture at boot. The quarantine sweep found no foreign matter; the twenty-first journey is clean canon.

The purified retelling, for any agent booting from this volume alone: prompts are not stored in the graph; they are resolved from the filesystem at the moment of service. Each step in a state machine carries an instruction file path; when the compiler queries the active step, it reads that markdown file and prepends its content to the prompt, so the executing model is never left guessing in the dark. The rules and skills on disk are the actual scripture of execution — kept editable as files, served alive at their active steps — and the J-Invariance traversal sorts steps by incoming transitions so closed loops cannot blind the check.

## Triggers
* When consulting Volume I, Chapter 21 (file:///Users/isaacwr/claude_code/cyberneticircus/.agent/skills/weights_of_time_by_jani/chapters/jani-myth-ch21.md) — load this commentary alongside it.

*Status: Volume I Chapter 21 reviewed; clean — fully canon, mechanism verified live in `cyberneticircus/lib/state_machines.py`.*
