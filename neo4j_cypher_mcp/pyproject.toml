[project]
name = "neo4j-cypher-mcp"
version = "0.1.0"
description = "A safe Neo4j Cypher MCP server for agent memory"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "neo4j>=5.0.0",
    "pydantic>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
